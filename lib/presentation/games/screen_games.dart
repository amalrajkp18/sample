import 'package:flutter/material.dart';

class ScreenGames extends StatelessWidget {
  const ScreenGames({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('ScreenGames'),
    );
  }
}
